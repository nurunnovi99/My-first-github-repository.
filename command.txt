# one time each project to start git
git init

# 2. every time to add
git add .

git commit -m "first commit"
# every time you commit
git branch -M main
# stay connected always
git remote add origin https://github.com/nurunnovi99/My-first-github-repository..git
git push -u origin main